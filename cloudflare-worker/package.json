{
  "name": "claude-code-routine-trigger-worker",
  "version": "0.0.0",
  "description": "Cloudflare Workers cron port of claude-code-routine-cron — fires Claude Code routines on a schedule.",
  "type": "module",
  "private": true,
  "license": "Apache-2.0",
  "packageManager": "pnpm@11.1.1",
  "main": "worker.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild", "sharp", "workerd"]
  },
  "devDependencies": {
    "wrangler": "^4.94.0",
    "vite": "^6.0.0",
    "vitest": "^4.1.7",
    "@cloudflare/vitest-pool-workers": "^0.16.9"
  }
}
